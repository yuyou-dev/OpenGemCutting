interface:
  display_name: "OpenGemCutting Start"
  short_description: "Install, verify, run, and open the faceting workbench."
  default_prompt: "Use $opengemcutting-start to install and open OpenGemCutting."
