# 更新日志 · Changelog

当前交付见文档索引与验收记录；以下 RC 和旧版本条目是当时的历史状态，不覆盖现行规则。

## 1.0.0 — 2026-09-11

- README 的社区区域增加余博士开源技术群企业微信二维码与联系入口。

- 明确可选 Companion 的 macOS/Linux 启动范围，其 Bash 启动回归只在对应平台运行；Windows 继续完整验证独立的设计 MCP。
- 仓库文本统一 LF，避免 Windows 检出转换破坏生成资产与能力文档的一致性。
- 测试入口显式枚举文件，兼容 Node 20/22 与 Windows shell；回归覆盖嵌套目录、含空格路径、空集合和失败退出码。
- 用户完成本机 Codex 对话创作测试并确认功能验收；对话与手动设计共用同一内核，基础网页保持独立运行。
- 界面、启动页和 PDF 从产品元数据读取版本；重新采集真实界面，统一 30 页手册、示例与展示素材。
- 安装、升级、卸载及支持状态各归单一文档；重做开源 README，移除旧 Alpha 展示图和重复截图，修复失效链接与过时交接。
- 文档链接检查纳入常规验收；同源同步检查遗留文件，防止已经删除的模块留在公开发行。
- MCP 构建校验涵盖服务实现、依赖锁文件和构建配置；升级后旧进程在调用时明确要求重启。
- 本地检查与公开发布分别记录；本条不表示已推送或已更新 Live Demo。


## 1.0.0-rc.2 — 2026-09-11 · 本地候选

- 同源引入可选本地 MCP、共享设计操作、能力与预设发现、完整设计 skill 和真实网页连接；支持预览、改型、撤销、保存与导出。
- 增加安装/升级提示词、setup 模块、macOS/Windows 依赖引导及项目内 Codex CLI 回退。设计插件与 Companion 职责分开；基础网页无需 Codex/MCP。
- 修复普通新建项目的请求变量误用和升级期间旧服务链接反馈；安装、接口与构建回归纳入检查。
- 设计师验收改为 5 个对话创作任务；手册同步到 30 页。macOS 与工程验证完成，Windows 实机和设计师评价待验收。
- 本地同步完成，尚未推送、发布或更新 Live Demo。

## 1.0.0-rc.1 — 2026-09-10 · 本地候选

- 公开发行同时加入凹晶体索引网格、四种原创异形底胚、跨标签页修订冲突保护，以及预设取消和 JSON 草稿保护。
- 切割助手采用右侧施工仪表与独立播放器，支持逐步、逐组、自动播放、倍速及平滑 45° 跟随。
- 新增统一琢型试作页面和 `design:review` 生成入口，真实几何四视对照及 JSON 下载。
- 开源发行包含参数化设计 skill、Design 插件安装流程与图文快速指南。
- 新外部文件明确资源预算；修复异步保存确认覆盖较新待保存快照的问题，增加视图渲染恢复入口。
- 支持范围按实际 Chromium / Codex 浏览器测试记录；Safari、Firefox 待验证。此版本尚未推送，待设计师验收。


## v0.8.2

### 设计工作流

- 新增切割助手（beta）：画布左上常驻「编辑 / 切割助手 β / 光学仿真」视口模式切换组，三个模式下可直接互切。切割助手按已提交 CUT STACK 的真实顺序从毛坯逐刀重放，支持“上一步 / 下一步”逐刀观看与“上一组 / 下一组”按图层跳转，底部播放条的可点进度条可直接跳转任意进度；只读不写历史，Escape 或“退出助手”原样恢复编辑现场。隐藏的图层不参与切割，未保存草稿不进入序列。
- 光学仿真材质预设扩充至 25 项：按宝石学折射率与色散收录常见宝石材料，彩色材质带体色与 Beer–Lambert 吸收，可直接对比不同材质下的亮暗与火彩。

### 修复与一致性

- 光学仿真画布内的滚轮缩放与触摸拖动改为原生非 passive 锁定，页面不再随画布手势抖动。
- 光学观察视角条移至画布右上角水平浮动，展开光学检查器时自动让位，不再与检查器重叠。
- 全系统的 tab 与分段选择器统一收敛为同一套浮动条样式，模式切换组与光学视角条共用基础语言。

### 文档

- 操作手册更新为 26 页，新增切割助手案例页并重新采集界面截图，同步帮助中心与入口描述。

## v0.8.1

### 兼容与维护

- JSON 导入兼容其他部署版本导出的 document-v1 文件：`$schema` 主机不同但格式相同的设计可以正常打开，导入后写回本版本标识。本地与其他托管版本的设计可以互相打开。
- 拖动行业角与深度滑杆时界面响应改进：拖动期间的派生几何延后渲染，撤销／重做可用状态不再重复校验历史。
- 清理无效代码与样式，收敛共用的数字格式化与文件名清洗工具，补充文档初始化和会话构造的测试覆盖；功能行为保持不变。

## v0.8.0

### 项目与布局

- 启动进入项目主页：快速新建、按项目名称组合搜索、真实几何项目卡片；用少量粉色标记、搜索图标和卡片箭头延续工作台主色。
- 已提交文档与光学材质自动保存到当前项目；刷新后回到主页选择继续。同一站点的有效旧备份一次性迁入项目列表，原备份保留，不会在每次启动时重复生成项目。
- 编辑页改为左侧参数、中央 3D、右侧 CUT STACK 三栏布局。CUT STACK 独立滚动，下方实时显示可切换的冠部／亭部顶底视图与固定侧视图。
- 光学仿真的视角切换条移至画布左上角，跟随宝石观察区；退出仿真仍恢复原编辑现场。
- 主页与编辑页新增光学实验室入口，保留当前项目上下文并支持返回；本版本仅提供空白布局，尚无实验工具。
- “GitHub 仓库”始终跟随品牌导航组，窄窗口不再独立挤到右侧。本机、GitHub Pages 及其他部署保持同一仓库入口。
- 操作手册更新为 25 页，补充项目主页、右侧预览和光学实验室说明，继续提供 7 份原创设计练习。

### 兼容与维护

- 页面内往返主页、编辑页与实验室保留当前 CUT 预览、相机与撤销历史；切换其他项目时明确处理未保存预览。刷新后的恢复仅包含已提交文档与材质。
- 保留 Meet / Jump、施工诊断、252 项预设、JSON / ASC / PDF、公开 JSON Schema、GitHub Pages 子路径和 Sites 构建支持。
- 项目自动保存受当前浏览器和站点地址限制，不能代替 JSON 长期归档；换浏览器、域名或本地端口需导入已导出的 JSON。
- 继续保留 MIT 与品牌权利说明，以及独立 Companion 和安装／升级／卸载流程。

## v0.7.2

### 设计工作流

- 品牌附近常驻“GitHub 仓库”；普通编辑、侧栏收起和光学仿真均可访问，新标签打开 https://github.com/yuyou-dev/OpenGemCutting。本机、GitHub Pages 与其他部署保持相同入口，当前设计留在原页。

- 预设库扩充至 252 项、27 类外形及 1,008 张真实几何四视图；组合外形、有效面数和 L/W 筛选，支持多关键词检索与每页 60 项浏览。
- 预设归档增加 SHA-256 来源校验、精确 96 齿兼容、最终实体拓扑和 JSON 往返检查，按最终有效面去重并保留关联来源；排除无法导入、不兼容或几何不完整的资料。

- 冠部／亭部的对称与自定义索引 CUT 支持真实棱上比例点及双 Meet；单 A 后可用 Jump 浏览第二点，双点在指定主分度求解角度与深度。
- 来源按实际施工顺序校验，修改、重排或隐藏后即时提示失效，保留已保存切面，由设计师重新编辑修复。
- 新增只读逐层试切助理和预形用途标记，支持前后造型比较、来源检查与首个失效工序定位。
- 图层常显“编辑”按钮；帮助管理键盘焦点与 Escape，比例工具只保留明确的完成入口。
- 全新 22 页设计师操作手册与 7 份原创练习，以冠高、棱面宽窄、双点会合和四向节奏解释功能用途。

### 兼容与维护

- JSON 与本地备份保存顶点／棱点 A/B、比例、主切面和预形用途；旧单点 Meet 文件继续兼容。PDF 说明来源与失效，ASC 继续只交换有效显式切面与折射率。
- 保留独立备份命名空间、公开 JSON Schema、GitHub Pages 子路径、MIT 与品牌权利说明，以及独立 Companion 和安装／升级／卸载流程。
- 统一当前构造、Escape、报告规范，清理无效模式锁定接口与误导性的重复取消按钮。角度拖动继续使用按需候选分类。

腰部与固定台面继续保持原角度锁定，不提供 Meet / Jump；不自动级联修复下游几何。完整设计与构造意图请保留 JSON。

## v0.7.1

本次 OpenGemCutting 更新从 v0.6.1 升级，包含 Meet / Jump 构造能力、有效面规则，以及恢复与交互修正。

### 新增与改进

- 冠部/亭部对称 CUT 支持单顶点 Meet 和 Jump。可显式选择并锁定顶点，或用 `J` / `Shift+J` 浏览交点；下一点预告区分仅接触、有效切面与覆盖影响，候选首尾不循环。
- 锁定 Meet 后深度自动求解；修改角度、基础索引、重复与镜像参数共用同一求解入口。不可达、来源失效和结构层失效有明确诊断，光学视图进出保留构造现场。
- CUT STACK 保留完整参数化工序，面数、切割指令、台账、PDF 和 ASC 统一读取最终有效面。普通冠部/亭部部分消面时提示，整层消失前确认；结构层整体失效与零有效面新 CUT 阻断提交。
- 已提交文档与光学材质自动本地备份；启动提示恢复或开始新设计。各页面独立备份，恢复列表支持刷新、选择与确认删除，恢复载入可一次撤销；写入失败提供重试和 JSON 导出。
- JSON、ASC 与 PDF 明确排除未保存预览。JSON 保留完整工序和已锁 Meet 元数据；PDF 标注 Meet 来源或失效状态，ASC 预检说明构造意图不随显式切面交换。
- 更新 13 页 A4 操作手册与真实界面截图，补充定位、有效面和恢复流程。

### 修复

- 修复复杂预设新建 CUT 时调角度卡顿：Jump 仅对显示/选中的候选进行精确分类，新建预览复用已提交实体，避免每次拖动重算全部候选和已保存切面。

- 修复刻面台账和弹窗的 Escape、Jump 快捷键影响底层 CUT 的问题；统一焦点约束与关闭后焦点返回。
- 光学物理与计算参数进入文档历史；载入预设、撤销/重做、JSON 与 ASC 使用同一材质值，并兼容旧 ASC 折射率字段。
- 图层名称与参数选择分开；点击名称或双击可改名，Enter/失焦提交、Escape 取消，后续参数保存保留自定义名称。
- 统一恢复和通用弹窗的圆角、边框、间距、按钮与选中态；修复长列表滚动、底部操作、空态重复操作、删除确认、文件菜单方向、错误提示遮挡和 Jump 预告拥挤问题。

### 升级说明

- 继续保留 GitHub Pages 子路径交付、公开 JSON Schema、MIT 许可与品牌权利说明，以及独立的 Companion、安装/升级/卸载 runbook 和社区入口。
- 本地备份只包含已提交文档和材质；未保存 CUT 草稿、群组预览、相机、观看参数和旧撤销历史不跨刷新恢复。备份受浏览器与站点地址限制，JSON 仍用于长期归档。
- Meet 当前只支持冠部/亭部对称 CUT 的单顶点定位，不包含腰部、固定台面、自定义索引、Edge Meet 或双 Meet。

## v0.6.1

- 新增 Codex 一句话安装、升级与启动流程，以及应用和 Companion 独立的生命周期说明。
- 提供 OpenGemCutting Companion：社区浏览、反馈草稿、本地改动检查与 PR 准备；外部写入需最终预览与明确确认。
- 保留完整图解手册、公开部署与品牌许可说明。
