# Axial Workbench 设计规范

## 视觉基准

| 参考 | 权威范围 | 状态 |
| --- | --- | --- |
| `docs/assets/design/selected-option-2.png` | 整体布局与精密仪器语言 | 历史基准，当前功能优先 |
| `docs/assets/design/toolbar-option-3.png` | 顶部命令条密度与层级 | 当前基准 |
| `docs/assets/design/gizmo-radial-gimbal-bridge.png` | 单层 CUT 串联机械臂结构 | 当前基准 |
| `docs/assets/design/optics-focus-simulation.png` | 聚焦光学仿真布局、密度与层级 | 当前基准 |

群组整体变换暂无独立效果图，以已落地界面和本文规范为真值。任何旧截图只解释其权威范围内的设计语言，不覆盖后续确认的功能决策。

## 视觉语言

- 背景 `#ffffff`；主墨色 `#111111`；次级文字 `#666666`；发丝线 `#c9c9c9`。
- p5 粉色 `#ed225d` 用于主操作与单层编辑，构造蓝用于新建刻面和角度桥架。群组升降使用紫色、比例使用青绿色、旋转使用暖金色；普通几何保持中性。
- UI 使用 Noto Sans SC；参数和值使用 IBM Plex Mono，并采用等宽数字。
- 结构以 1px 发丝线、紧凑圆角和克制留白为主。阴影只用于区分浮动表面，保持轻且无装饰性；不使用装饰渐变。主页项目网格采用同一细线语言，卡片只承载真实设计，不把编辑器参数改成卡片矩阵。
- 3D 几何边线与 helper 线宽保持屏幕空间细线，不随缩放显得越来越粗。
- 正文与辅助标签不得小于 `9px`，操作按钮不得小于 `10px`。

## 布局

- v0.8.0 编辑页为三栏：左侧可收起的 292px 参数区、中间弹性 3D 画布、右侧 304px 的 CUT STACK 与正交预览。1280px 以下侧栏分别收紧为 264px / 284px。主画布保持独立可操作区域。
- 顶部为贯穿三栏的固定菜单带：左侧品牌返回主页，接项目主页/光学实验室导航与 GitHub 仓库，右侧保留既有视图、名称、显示、文件与工具。CUT STACK 固定到右栏上部，图层列表独立滚动，区域切换与底部会话提交保持可见。沿用 `--rail-h: 42px`、`--rail-pad: 6px`、`--control-h: 30px`。
- 命令区常驻视图菜单、可编辑切型名称与面数、显示模式、文件、撤销/重做和更多菜单；它与页面导航在同一菜单带内。显示模式菜单包含实体、X-ray 与“光学仿真”入口；光学不是永久工作区按钮。JSON、GemCad ASC 与 PDF 的导入导出位于文件菜单。
- GemCad ASC 使用同一个文件菜单进入，不增加常驻工作区。选择文件后先打开居中的紧凑预检层，清楚区分“可无损转换 / 可转换但有警告 / 不可转换”，并显示齿轮映射、层/面数、折射率、L/W、统一比例和带行号诊断；严重错误禁用确认，警告态使用“仍然导入/导出”的明确文案。
- 预设琢型从“文件”菜单打开居中资料浏览层，不增加顶部常驻按钮、左侧抽屉或新的工作区模式。浏览层延续白底、黑色发丝线、紧凑双语标签和克制浮层阴影；绿色只用于标识资料入口/通过校验，粉色只用于最终“载入此琢型”主操作。
- 预设浏览层支持名称／作者／来源多关键词、外形、有效刻面数与真实实体 L/W 的组合筛选，显示各类别匹配数量、筛选结果数与清除入口；面数区间为 ≤60、61–100、101–160、>160，L/W 区间为 ≤1.05、(1.05,1.30]、(1.30,1.60]、>1.60，不根据数字虚构风格或实际切磨难度。列表每页最多 60 项，底部常显范围、页码与上一页／下一页；更改筛选回到第一页。
- 预设列表只承担查找与选择，右侧详情使用真实几何批量生成的 `45° 轴测 / 顶视 / 底视 / 正视` 四视图，并同时展示外形、面数、层数、源分度、L/W、设计者及来源声明。预览固定白底黑线，不能使用装饰图、占位图或与文档无关的示意模型。
- 预设详情的四视图轨道保留明确的最小高度（桌面两行306px、窄窗单列598px、小屏两行266px），空间不足时滚动详情，不让图像覆盖面数、层数、分度或载入入口。
- 冠部/腰部/亭部切换属于 `CUT STACK`，参数抽屉中不重复出现。
- 历史、刻面台账、设置和帮助按需浮动显示。刻面台账是居中覆盖层，不是底栏或永久侧栏。
- 启动资源完成后进入项目主页。启动加载页是应用外壳的一部分：在模块、页面与字体准备期间以白底、真实品牌标志、黑色标题、灰色产品线和一条粉色进度反馈覆盖工作区。它只表达“资源准备中”，资源完成后短暂淡出，并在减少动态效果偏好下停用循环动画。
- 帮助仍从“更多工具”进入，不新增常驻按钮。帮助中心使用与预设库一致的居中资料层：左侧五个任务主题，右侧展示步骤、事实或快捷键，底部提供独立 A4 PDF 手册与返回工作台操作。打开、切换与关闭帮助不改变文档、历史或 CUT 会话。
- 可聚焦画布不得出现包围整个 3D 区域的浏览器蓝色轮廓；键盘操作能力必须保留。

## 控件与信息层级

- 行业角、切入深度、96 分度、重复、镜像轴偏移和自定义索引属于同一个 CUT 配置，应紧凑排列。行业角的滑杆、数值框、行内编辑和 3D 手柄统一以 `0.01°` 为最小步进，并显示两位小数。
- 冠部/亭部对称及自定义索引 CUT 在深度控件下提供一个紧凑“定位与约束”区，包含“上一交点 / 下一交点 / 选择顶点 / 选择棱”、候选序号、来源、深度、分类及“锁定 / 解除 Meet”。它属于现有参数抽屉，不新增工作区、常驻导航或 CUT 第五态；腰部和固定台面不显示此区。
- 视口拾取须显式进入“选择顶点／选择棱”，点选后先预览、再由按钮锁定；Jump 也可沿候选路径直接预览与锁定。单 A 锁定后，深度滑杆、数字框、行内深度与粉色深度杆统一只读，并说明自动求解；双 Meet 同时锁定角度与深度。解除使用明确按钮，棱比例工具仅保留“完成比例调整”，退出工具不回滚比例。不可达、来源失效或零有效面必须就地给出文字诊断并禁用保存，不能只改变颜色。
- Jump 使用 `J` 前进、`Shift+J` 后退，按钮和快捷键具有相同的非循环边界行为。候选信息必须明确区分“仅接触 / 形成有效切面 / 产生覆盖影响”；仅接触与零有效面候选可以查看、锁定定位，但保存操作保持禁用。
- 未锁 Meet 时，视口持续显示严格位于当前深度之后的一个“下一点”预告：使用空心菱形和“下一点 · n/N”文字，与已经跳到的候选及锁定 Meet 明确区分。侧栏同步显示其来源与所需深度；预告本身不得移动切面。
- 96 分度采用一行高的水平 `IndexTape`：点击或拖动吸附整数齿，步进按钮 ±1，读数支持精确输入。
- 空闲态不显示常驻草稿卡，也不在 CUT 标题栏重复放置“新建”。新建和编辑态显示轻量会话条，并始终提供可见的取消/退出；群组态在 `CUT STACK` 内提供取消。
- `CUT STACK` 底部保持固定高度的会话槽位：空闲态显示绿色“新建切割图层”入口；新建态原位切换为粉色状态条，显示「新建{区域}」及「有效 n / 生成 N 面」，并提供「取消」与「加入序列 · {有效面数} 面」双按钮；编辑态显示「编辑(· 未保存)」和有效/生成统计并提供「放弃」与「保存」双按钮。标题栏不放会话关闭按钮，左侧参数抽屉不显示重复状态卡，也不放提交按钮；保存/放弃主操作只出现在会话槽位内。
- `CUT STACK` 底部新建图层入口使用低饱和绿色整行按钮、圆形加号和一行用途说明，与粉色保存操作明确区分；绿色只表达“创建”，不得用于保存或破坏性操作。
- `CUT STACK` 每个已保存图层右侧常显带边框的“编辑”文字按钮；面数位于按钮上方，名称继续用于改名，参数和面数保留兼容编辑入口。编辑按钮直接消费 `canSelectLayers`，活动会话中禁止切换图层；当前图层显示“编辑中”，并保持清晰的粉色选中反馈。按钮使用统一控件高度与可见键盘焦点；长名称省略且悬停可读全名，预形与 Meet 标签不得挤压编辑按钮。
- 切型名称使用命令条内的真实文本输入框：聚焦时全选，`Enter` 或失焦保存，`Escape` 恢复原名。改名写入统一命令历史，可撤销/重做；名称输入的键盘事件不得退出或提交 CUT 会话。空名称不提交，JSON、ASC 与 PDF 继续读取文档名称这一唯一数据源。
- 切割指令按亭部、腰部、冠部三栏显示最终实体中仍有有效面的图层与索引，台面放在冠部末尾；已被完全覆盖的参数化工序只保留在 `CUT STACK` 和 JSON 中。每行统一为粗体操作前缀、两位小数行业角和可自然换行的完整索引串。
- 品牌标题固定为黑色“切磨工作台”；`Alpha` 和产品线为灰色；品牌拼写固定为 `SUVA`。

## 3D 交互

- 新建/编辑 CUT 使用安装在分度盘上的世界空间串联机械臂：蓝色 0–90° 弧形桥架位于当前索引的竖直径向平面内，半径与外圈 96 分度圆完全相同，二者共用世界原点作为圆心；桥架 90° 端点以竖直切线直接锚在外圈固定轴承上，不使用额外连接段。
- 桥架滑块只接一根粉色直杆；直杆沿圆弧半径方向，始终垂直局部切线，并作为切割平面的法线。切割平面与直杆垂直；改变深度时圆弧和角度滑块不漂移，整张平面只沿该法线平移。直杆末端圆标是平面控制柄，不代表宝石上的几何点。
- 冠部桥架向上、亭部桥架向下，不能合并成含义模糊的 180° 量角器。
- 外圈石墨分度盘只改变基础索引；内圈暖金分度盘只改变镜像轴偏移。两者均吸附到 96 齿整数位置。
- 机械臂、轴承和双环跟随宝石在世界空间旋转，数值徽标保持可读。Gizmo、粉色深度杆和红色主编辑平面始终绑定 `baseIndex` 对应成员，不能绑定排序后的第一个重复成员。
- 主切割面标签固定显示“主切面 · Ixx”，其中索引严格来自 `baseIndex`。Jump 候选使用空心顶点标记，已锁 Meet 使用实心顶点与锁形标记，不可达或失效目标使用断线标记和文字；三种状态不得只靠颜色区分。
- 新建冠部和亭部 CUT 时深度显示 `0.000`，切割平面位于毛坯支撑位置；用户拖动深度或执行 Jump 后才向主体切入。编辑层不套用该默认值。
- 只有显式 Meet 拾取模式才显示并响应当前可见、未被实体遮挡的 base-solid 顶点命中区；空白点击和相机旋转、缩放、平移不创建或解除 Meet。实体后方的顶点不得在屏幕层补画或接受命中。
- 旋转重复为 N 时，全部 N 个切面参与几何裁切；实体模式只显示一张红色主编辑平面，其他重复成员仅在 X-ray 中以次级样式出现。非零镜像轴偏移产生的第二轨道不得夺取主 Gizmo。
- 深度没有固定上限，范围根据毛坯尺寸和当前值扩展；暂时切得过深时，helper 和预览仍可恢复操作。
- 默认实体模式优先保证宝石可读性；X-ray 是显式切换状态。构造线和派生切面预览保持克制，不遮蔽主要几何。
- 群组整体变换在一个操纵杆中组合三种自由度：紫色平面/手柄调整 `ΔZ`，青绿色平面/手柄以腰线为基准调整高度比例，暖金色 96 齿环调整整数旋转。石墨灰平面表示固定腰线基准。
- 实体模式下，群组控制面、连接轴、手柄和标签统一接受实体深度遮挡；被遮住的部分既不在屏幕层补画，也不参与命中。控制面以轻微法线偏移规避腰线或台面共面闪烁。

## 状态语义

- CUT 会话只有空闲、新建、编辑、群组四态；Meet / Jump 只是新建或编辑中的构造子状态。所有非空闲态必须提供显式取消和 `Escape`，取消不写历史；`Escape` 按 `state-contract.md` 规定的模态、构造工具、第二点预览和 CUT 层级退出，不另设固定按键次数。
- 空闲态点击区域标签只切换筛选和默认参数，只有列表底部 `+` 能进入新建。编辑或新建中切换区域会放弃当前会话，并以新区域默认值开始草稿；已保存图层不受影响。
- 新建态立即显示淡蓝刻面、主切割面和 Gizmo。编辑态一进入就显示淡粉活动刻面、主切割面和 Gizmo；未修改时实体仍是已提交几何，参数变化后才显示未保存实体预览。
- 活动会话期间不允许通过点面、图层、历史或台账静默切换目标。保存编辑在原序列位置替换图层，群组整体变换作为一个命令提交。
- 覆盖、空切与最终有效面的判定以 `state-contract.md` 为语义真值。界面只负责按结果表达：普通 C/P 部分消面使用参数区内联提示，整层消失使用提交前确认；硬阻断只显示诊断而不提供继续按钮。`CUT STACK` 始终显示参数化工序，面数和刻面台账只显示完整保存实体的最终有效面；临时隐藏不改写这些文档派生结果。
- 编辑态的活动图层即使因会话锁定而不可再次点击，行内名称、参数摘要和面数仍保持深色与完整不透明度；粉色底和左侧粉色标记表达选中。其他不可操作图层才使用浅灰弱化，避免把活动层误读为未选中。
- 载入预设会替换当前文档，但必须作为一次统一文档命令写入历史，因此可用一次撤销恢复载入前现场；打开、搜索、筛选和关闭预设浏览层本身不写历史，也不是 CUT 第五态。

## 聚焦光学仿真

- 光学仿真是覆盖编辑器的临时聚焦视图，不是 CUT 第五态。它可从空闲、新建、编辑或群组态进入；进入与退出本身不写历史、不提交或取消草稿。
- 进入后隐藏左侧参数抽屉、`CUT STACK`、历史/台账和全部 CUT helper、选中/预览材质，只保留大画布、统一顶部品牌/导航菜单带、仿真命令与右侧光学检查器。检查器可独立收起，收起不退出仿真。
- 顶部必须提供粉色“退出仿真”；`Escape` 只退出仿真。退出后恢复进入前的侧栏展开状态、`CUT STACK` 折叠状态、CUT 会话身份与脏参数、Meet / Jump 构造子状态、Gizmo 和编辑相机现场。
- 仿真几何始终读取当前可见实体：空闲态使用已提交几何，新建/脏编辑使用未保存预览，群组态使用整体变换预览。不得使用另建的示例钻石网格代替当前文档。
- 材质栏只放物理参数：预设、折射率（无色钻石默认 `2.417`）、色散（默认 `0.044`）、由折射率派生的临界角、体色与 Beer–Lambert 吸收。渲染至少计算介质 Fresnel、折射、全反射、内部多次反射与 RGB 色散。
- 画布左上浮动视角条显示透视、台面、亭部、正视和侧视，控制当前宝石观察；顶部保留文档信息、退出仿真与实时状态。浮动条与检查器不重叠，小屏检查器下移。台面与亭部是严格沿世界 `±Z` 的轴向观察位，但所有观察位必须使用同一透视镜头和取景尺度，不得在轴向切换成正交投影。轨道拖拽可连续穿越 `±90°` 极点，不能在接近垂直时吸附、锁死或改变主体大小。
- 体色代表单位路径的透射色，必须转换为逐通道 Beer–Lambert 吸收并与中性吸收系数相加；中性吸收为 `0` 时选择黄、粉、蓝、绿仍应产生可见体色。快捷色下方提供按需展开的系统调色盘和十六进制输入。
- 视图栏只放观看参数：摄影棚环境、背景、曝光和环境旋转。视图参数不得改变折射率、色散或吸收常量；界面需明确标示 `VIEW ONLY`。
- 视图环境包含用于对称光学检查的“八心八箭观察镜”。它是结构化红白观察环境，不改变材质或几何，也不替代实验室等级判定。
- 光学画面保持白色精密仪器语言，主体使用中性雾白摄影棚；物理高光与火彩来自当前凸多面体的光线求交和环境采样，不以普通高光贴图冒充光学结果。

## PDF 图层示意

- 参数页按仍有最终有效面的保存图层逐组输出，不把同一区域的 C1/C2 或 P1/P2 合并成一张连续大表；超出单页容量时同组分页。
- 每个有效图层页都保留对应示意图：冠部和台面使用顶视，亭部使用底视，腰部使用正视；该图层的最终有效面以工作台粉色半透明填充和粉色边界标识。
- 示意图、组参数和逐面表必须出现在同一页，分页页仍重复示意图，保证数据与空间位置可以直接对应。
- 已锁且来源有效的 Meet 在对应图层参数区显示构造类型与来源；棱点包含比例，双 Meet 分别列出 A/B，预形用途单独标注。来源失效时明确说明当前切面仍以显式参数为准，不能把失效来源当作有效构造输出。未锁定 Jump 不进入 PDF。PDF 与 ASC 只列出最终实体中的有效面，被覆盖面不进入图层表、面数或 ASC；ASC 导出预检仍须提示已提交 Meet 的构造意图会被省略。

## 操作手册

- 操作手册与单个切型的技术报告职责分开：技术报告服务工艺交付，操作手册服务设计师创作与学习。章节从具体设计目标出发，用真实切型串起操作、可观察的造型变化和设计取舍；对照图保持相同视角，光学比较保持材质与环境一致，不把亮度或面数直接等同设计质量。
- 手册固定使用 A4 纵向、真实工作台截图、可搜索中文字体、黑色发丝线、粉色章节标识与绿色提示块；用目录、连续章节编号、截图标注、任务步骤、局部说明和检查表建立阅读节奏，不得退化为平均分配的“整张截图加短说明”。封面使用独立品牌版式且不显示正文页头；第 2 页起页头统一显示品牌标识、“切磨工作台 Alpha”和“SUVA · FACET 96”，结尾使用克制的许可与致谢区块。
- 练习包含可导入的起始文件与结果文件、操作路径、预期视觉结果及开放的设计判断；工程测试与设计师体验验收分开记录。案例和截图在手册生成时校验，避免文案、文件与界面脱节。
- 光学仿真图例必须先载入经过验证的完成切型，并至少提供透视与台面轴向两种观察视角；观察位与画布视角条一致，共用同一透视镜头，不使用正交投影；不得再使用默认毛坯充当光学成果图。
- 手册源截图保存在 `docs/manual/screenshots/`，至少使用 2× 像素密度，必须完整呈现整页或功能区；排版使用等比完整适配，不得为填满画框裁掉图标、控件或面板边界。输出固定为 `public/manual/facet-96-operation-manual.pdf`，版本读取 `package.json`；运行 `npm run manual:build` 重新生成。界面、截图可见 UI、产品规则或版本号变化后，必须重采受影响截图并重新生成手册，不允许以占位图或旧界面充数。

## 工作台交互与反馈

- 图层名称与切割编辑是分离的控件：每行常显的“编辑”文字按钮为 CUT 编辑主入口，参数和面数保留快捷入口；点击名称直接改名，也支持双击。名称输入 Enter / 失焦提交，Escape 取消，改名后调整参数不得重置名称。
- 命令条明确显示已提交文档的有效面数；参数区说明当前实体的有效刻面与残余毛坯面；CUT 会话槽位显示「有效 n / 生成 N 面」。空切不出现在工艺索引清单中。
- 双 Meet 锁定后上一/下一第二点禁用并说明解除方式；单 Meet 可继续浏览第二点。首尾按钮按候选边界禁用。下一点预告同时显示「仅接触 / 形成有效切面 / 覆盖已有面」。
- 刻面表及通用对话框管理焦点；Escape 只关闭浮层，恢复之前的触发控件，不取消底层 CUT。
- 有活动 CUT 时导出 JSON 显示范围确认；ASC 与 PDF 在现有预检中说明仅导出已提交文档、忽略当前预览，不增加隐式保存。

- 本地恢复沿文件菜单打开居中列表，不新增工作区；显示设计名称、备份时间和层数，并按时间倒序。启动进入主页列出迁入的本地项目；备份范围、独立副本及站点存储边界以简短文字说明。文件菜单显示备份状态；失败时显示持久提示与重试/导出入口。

## 浮层与本地恢复控件

- 通用确认/导出浮层与帮助、预设库共用白底、7px 圆角、石墨细边框、轻阴影和中性模糊遮罩。标题、正文、底部操作使用统一 20px 内边距；小窗口为 16px。主次按钮统一 12px 字号与 34px 高度，禁止依赖原生按钮样式。
- 浮层头尾固定，内容在剩余高度内滚动；长列表和长名称不得推出底部操作。空态只提供一个返回/开始操作，不重复“知道了”。打开时聚焦对话框本体，Tab/Shift+Tab 不离开浮层，关闭后返回菜单触发器。
- 本地恢复使用两行恢复范围说明、带数量与刷新入口的列表标题、粉色整行选中态和粉色单选控件；列表与工具栏必须对齐正文内边距。删除入口弱于恢复；删除确认显示目标名称，底部只提供“保留备份 / 确认删除备份”，Escape 先取消删除确认。
- 文件菜单向触发器左侧展开，菜单状态说明自然换行；备份失败提示使用规范化按钮并位于模态遮罩之下，不遮挡导出/恢复弹窗。
- Jump 预告分两行展示分类和深度/来源，避免新增分类挤掉来源；候选辅助标签不小于 9px。面数按钮背景透明，避免出现原生灰色矩形。

## v0.7.2 定位与施工检查

- 冠／亭对称与自定义索引复用原“定位与约束”区，明确 A／B、顶点／棱上点、比例和来源；不新增永久面板。锁定以文字与控件只读共同表达；双点说明“自动求解角度与深度”。
- 候选与已锁 A／B 分开，保留明确锁定、解除 A／B 和全部解除入口；棱比例快选与数值输入复用现有按钮／数字控件，不依赖浏览器默认蓝色样式。
- 自定义主切面使用索引集合内的显式选择，展示“主切面”与 Ixx；预形用途为普通冠／亭层的轻量勾选，不抢占粉色保存操作。
- 图层 stale 用可读短标签及原因，不只变色；活动行文字保持深色。3D A／B 使用字母及既有锁定／候选标记，不改变机械臂色彩与深度遮挡。
- 试切助理从更多工具按需打开，复用通用模态白底、7px 圆角、20px 内边距、12px／34px 按钮和固定头尾；列表／内容内部滚动，小窗口按现有规范适配。显示真实阶段实体、当前步骤及首次失效入口，明确这是施工检查。
- 新增区域的来源长文本允许换行，flex/grid 子项 min-width:0；不让文字推宽抽屉或把按钮推出浮层。视觉回归以同状态、同窗口前后截图为证据，覆盖 1600×900、1280×800、1024×768 与 200% 缩放。

## 常驻 GitHub 仓库入口

主页右上与编辑/实验室的顶部菜单带常驻“GitHub 仓库”文字、图标和新标签箭头，沿用石墨描边白底，绿色用于悬停/键盘焦点。编辑页将它与页面导航放在同一紧凑组，位于品牌与命令之间；窄屏整组换行，不将仓库按钮单独推到右边，不随参数侧栏收起或光学仿真隐藏。链接另开标签，不退出文档、不改变CUT会话或历史；目标固定为 OpenGemCutting，不按版本、域名、路径或环境变量分流。

## v0.8.0 三页结构

- `docs/assets/design/v0.8.0-workspace-sketch.jpg` 是主页、三栏编辑页与空白光学实验室的信息结构真值；颜色、字体、控件语义继续以本文与现有界面为真值，草图不定义新的视觉风格。
- 主页顶栏显示 SUVA / FACET 96、此浏览器项目数、GitHub 与更多工具。主区域居中显示真实猫标志、黑色工作台名、灰色 Alpha、单行项目搜索；下方为项目列表标题、实验室/继续/新建入口及响应式项目网格。搜索目前仅查找项目名称，不显示尚未实现的 AI 操作。
- 项目卡使用真实保存文档的轴测几何、名称与更新时间；空列表不填虚构项目，搜索无结果与未创建项目分别提示。猫标志可作轻微浮动，尊重减少动态效果偏好；不改造品牌素材。
- 右栏上部 CUT STACK 的列表独立滚动，保留当前编辑行、固定底部新建/保存操作。下方两幅只读视图：上图“冠部 / 亭部”切换顶/底视，下图固定侧视。直接显示与主视口相同的当前实体，保留粉色编辑、蓝色草稿高亮；不显示机械臂，不响应旋转或CUT选择。
- 光学实验室沿用品牌菜单带与三栏边界，中央仅显示当前设计、实验室名称和准备中提示。实验工具留白，不放虚构指标、模拟结果或占位按钮。主页和编辑页可进入，有当前项目时提供返回编辑。
- 1100px以下顶部命令区换到第二行，保证全部入口可达；700px以下画布、参数、序列/预览上下排列，文档页面可滚动。桌面窄高窗口保留可滚动的完整工作区，不能裁掉保存或导航入口。

- 主页品牌粉只作为辅助：搜索图标与焦点、项目标题短竖标、卡片进入箭头使用现有粉色 token；工作台名称与几何保持黑白，绿色新建语义保持不变，不增加大面积粉色背景。
