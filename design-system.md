# Axial Workbench 设计规范

## 视觉基准

| 参考 | 权威范围 | 状态 |
| --- | --- | --- |
| `docs/assets/design/selected-option-2.png` | 整体布局与精密仪器语言 | 历史基准，当前功能优先 |
| `docs/assets/design/toolbar-option-3.png` | 顶部命令条密度与层级 | 当前基准 |
| `docs/assets/design/gizmo-radial-gimbal-bridge.png` | 单层 CUT 串联机械臂结构 | 当前基准 |
| `docs/assets/design/optics-focus-simulation.png` | 聚焦光学仿真布局、密度与层级 | 当前基准 |

群组整体变换暂无独立效果图，以已落地界面和本文规范为真值。任何旧截图只解释其权威范围内的设计语言，不覆盖后续确认的功能决策。

## 视觉语言

- 背景 `#ffffff`；主墨色 `#111111`；次级文字 `#666666`；发丝线 `#c9c9c9`。
- p5 粉色 `#ed225d` 用于主操作与单层编辑，构造蓝用于新建刻面和角度桥架。群组升降使用紫色、比例使用青绿色、旋转使用暖金色；普通几何保持中性。
- UI 使用 Noto Sans SC；参数和值使用 IBM Plex Mono，并采用等宽数字。
- 结构以 1px 发丝线、紧凑圆角和克制留白为主。阴影只用于区分浮动表面，保持轻且无装饰性；不使用渐变和卡片矩阵。
- 3D 几何边线与 helper 线宽保持屏幕空间细线，不随缩放显得越来越粗。
- 正文与辅助标签不得小于 `9px`，操作按钮不得小于 `10px`。

## 布局

- 只有一个永久控制面：可折叠的 322px 左侧参数抽屉。3D 画布始终是主区域。
- `CUT STACK` 位于画布左上；命令条位于右上。两者顶边对齐并共用 `--rail-h: 42px`、`--rail-pad: 6px`、`--control-h: 30px`。
- 命令条只常驻视图菜单、可编辑切型名称与面数、显示模式、文件、撤销/重做和更多菜单。显示模式菜单包含实体、X-ray 与“光学仿真”入口；光学不是永久工作区按钮。JSON、GemCad ASC 与 PDF 的导入导出位于文件菜单。
- GemCad ASC 使用同一个文件菜单进入，不增加常驻工作区。选择文件后先打开居中的紧凑预检层，清楚区分“可无损转换 / 可转换但有警告 / 不可转换”，并显示齿轮映射、层/面数、折射率、L/W、统一比例和带行号诊断；严重错误禁用确认，警告态使用“仍然导入/导出”的明确文案。
- 预设琢型从“文件”菜单打开居中资料浏览层，不增加顶部常驻按钮、左侧抽屉或新的工作区模式。浏览层延续白底、黑色发丝线、紧凑双语标签和克制浮层阴影；绿色只用于标识资料入口/通过校验，粉色只用于最终“载入此琢型”主操作。
- 预设列表只承担查找与选择，右侧详情使用真实几何批量生成的 `45° 轴测 / 顶视 / 底视 / 正视` 四视图，并同时展示外形、面数、层数、源分度、L/W、设计者及来源声明。预览固定白底黑线，不能使用装饰图、占位图或与文档无关的示意模型。
- 冠部/腰部/亭部切换属于 `CUT STACK`，参数抽屉中不重复出现。
- 历史、刻面台账、设置和帮助按需浮动显示。刻面台账是居中覆盖层，不是底栏或永久侧栏。
- 启动加载页是应用外壳的一部分：在模块、页面与字体准备期间以白底、真实品牌标志、黑色标题、灰色产品线和一条粉色进度反馈覆盖工作区。它只表达“资源准备中”，资源完成后短暂淡出，并在减少动态效果偏好下停用循环动画。
- 帮助仍从“更多工具”进入，不新增常驻按钮。帮助中心使用与预设库一致的居中资料层：左侧五个任务主题，右侧展示步骤、事实或快捷键，底部提供独立 A4 PDF 手册与返回工作台操作。打开、切换与关闭帮助不改变文档、历史或 CUT 会话。
- 可聚焦画布不得出现包围整个 3D 区域的浏览器蓝色轮廓；键盘操作能力必须保留。

## 控件与信息层级

- 行业角、切入深度、96 分度、重复、镜像轴偏移和自定义索引属于同一个 CUT 配置，应紧凑排列。行业角的滑杆、数值框、行内编辑和 3D 手柄统一以 `0.01°` 为最小步进，并显示两位小数。
- 96 分度采用一行高的水平 `IndexTape`：点击或拖动吸附整数齿，步进按钮 ±1，读数支持精确输入。
- 空闲态不显示常驻草稿卡，也不在 CUT 标题栏重复放置“新建”。新建和编辑态显示轻量会话条，并始终提供可见的取消/退出；群组态在 `CUT STACK` 内提供取消。
- `CUT STACK` 底部保持固定高度的会话槽位：空闲态显示绿色“新建切割图层”入口，新建或编辑态原位切换为粉色状态条，显示当前动作、面数及“取消新建 / 退出编辑 / 放弃修改并退出编辑”。标题栏不放会话关闭按钮，左侧参数抽屉不显示重复状态卡。底部保存/更新主按钮的位置和粉色主操作语义保持不变。
- `CUT STACK` 底部新建图层入口使用低饱和绿色整行按钮、圆形加号和一行用途说明，与粉色保存操作明确区分；绿色只表达“创建”，不得用于保存或破坏性操作。
- 切型名称使用命令条内的真实文本输入框：聚焦时全选，`Enter` 或失焦保存，`Escape` 恢复原名。改名写入统一命令历史，可撤销/重做；名称输入的键盘事件不得退出或提交 CUT 会话。空名称不提交，JSON、ASC 与 PDF 继续读取文档名称这一唯一数据源。
- 切割指令按亭部、腰部、冠部三栏显示所有图层，台面放在冠部末尾。每行统一为粗体操作前缀、两位小数行业角和可自然换行的完整索引串。
- 品牌标题固定为黑色“切磨工作台”；`Alpha` 和产品线为灰色；品牌拼写固定为 `SUVA`。

## 3D 交互

- 新建/编辑 CUT 使用安装在分度盘上的世界空间串联机械臂：蓝色 0–90° 弧形桥架位于当前索引的竖直径向平面内，半径与外圈 96 分度圆完全相同，二者共用世界原点作为圆心；桥架 90° 端点以竖直切线直接锚在外圈固定轴承上，不使用额外连接段。
- 桥架滑块只接一根粉色直杆；直杆沿圆弧半径方向，始终垂直局部切线，并作为切割平面的法线。切割平面与直杆垂直；改变深度时圆弧和角度滑块不漂移，整张平面只沿该法线平移。直杆末端圆标是平面控制柄，不表达宝石上的 meet 点。
- 冠部桥架向上、亭部桥架向下，不能合并成含义模糊的 180° 量角器。
- 外圈石墨分度盘只改变基础索引；内圈暖金分度盘只改变镜像轴偏移。两者均吸附到 96 齿整数位置。
- 机械臂、轴承和双环跟随宝石在世界空间旋转，数值徽标保持可读。Gizmo、粉色深度杆和红色主编辑平面始终绑定 `baseIndex` 对应成员，不能绑定排序后的第一个重复成员。
- 旋转重复为 N 时，全部 N 个切面参与几何裁切；实体模式只显示一张红色主编辑平面，其他重复成员仅在 X-ray 中以次级样式出现。非零镜像轴偏移产生的第二轨道不得夺取主 Gizmo。
- 深度没有固定上限，范围根据毛坯尺寸和当前值扩展；暂时切得过深时，helper 和预览仍可恢复操作。
- 默认实体模式优先保证宝石可读性；X-ray 是显式切换状态。构造线和派生切面预览保持克制，不遮蔽主要几何。
- 群组整体变换在一个操纵杆中组合三种自由度：紫色平面/手柄调整 `ΔZ`，青绿色平面/手柄以腰线为基准调整高度比例，暖金色 96 齿环调整整数旋转。石墨灰平面表示固定腰线基准。
- 实体模式下，群组控制面、连接轴、手柄和标签统一接受实体深度遮挡；被遮住的部分既不在屏幕层补画，也不参与命中。控制面以轻微法线偏移规避腰线或台面共面闪烁。

## 状态语义

- CUT 会话只有空闲、新建、编辑、群组四态；所有非空闲态必须提供显式取消和 `Escape`，取消不写历史。
- 空闲态点击区域标签只切换筛选和默认参数，只有列表底部 `+` 能进入新建。编辑或新建中切换区域会放弃当前会话，并以新区域默认值开始草稿；已保存图层不受影响。
- 新建态立即显示淡蓝刻面、主切割面和 Gizmo。编辑态一进入就显示淡粉活动刻面、主切割面和 Gizmo；未修改时实体仍是已提交几何，参数变化后才显示未保存实体预览。
- 活动会话期间不允许通过点面、图层、历史或台账静默切换目标。保存编辑在原序列位置替换图层，群组整体变换作为一个命令提交。
- 编辑态的活动图层即使因会话锁定而不可再次点击，行内名称、参数摘要和面数仍保持深色与完整不透明度；粉色底和左侧粉色标记表达选中。其他不可操作图层才使用浅灰弱化，避免把活动层误读为未选中。
- 载入预设会替换当前文档，但必须作为一次统一文档命令写入历史，因此可用一次撤销恢复载入前现场；打开、搜索、筛选和关闭预设浏览层本身不写历史，也不是 CUT 第五态。

## 聚焦光学仿真

- 光学仿真是覆盖编辑器的临时聚焦视图，不是 CUT 第五态。它可从空闲、新建、编辑或群组态进入；进入与退出本身不写历史、不提交或取消草稿。
- 进入后隐藏左侧参数抽屉、`CUT STACK`、历史/台账和全部 CUT helper、选中/预览材质，只保留大画布、紧凑品牌条、仿真命令条与右侧光学检查器。检查器可独立收起，收起不退出仿真。
- 顶部必须提供粉色“退出仿真”；`Escape` 只退出仿真。退出后恢复进入前的侧栏展开状态、`CUT STACK` 折叠状态、CUT 会话身份与脏参数、相遇目标、Gizmo 和编辑相机现场。
- 仿真几何始终读取当前可见实体：空闲态使用已提交几何，新建/脏编辑使用未保存预览，群组态使用整体变换预览。不得使用另建的示例钻石网格代替当前文档。
- 材质栏只放物理参数：预设、折射率（无色钻石默认 `2.417`）、色散（默认 `0.044`）、由折射率派生的临界角、体色与 Beer–Lambert 吸收。渲染至少计算介质 Fresnel、折射、全反射、内部多次反射与 RGB 色散。
- 仿真命令条直接显示透视、台面、亭部、正视和侧视。台面与亭部是严格沿世界 `±Z` 的轴向观察位，但所有观察位必须使用同一透视镜头和取景尺度，不得在轴向切换成正交投影。轨道拖拽可连续穿越 `±90°` 极点，不能在接近垂直时吸附、锁死或改变主体大小。
- 体色代表单位路径的透射色，必须转换为逐通道 Beer–Lambert 吸收并与中性吸收系数相加；中性吸收为 `0` 时选择黄、粉、蓝、绿仍应产生可见体色。快捷色下方提供按需展开的系统调色盘和十六进制输入。
- 视图栏只放观看参数：摄影棚环境、背景、曝光和环境旋转。视图参数不得改变折射率、色散或吸收常量；界面需明确标示 `VIEW ONLY`。
- 视图环境包含用于对称光学检查的“八心八箭观察镜”。它是结构化红白观察环境，不改变材质或几何，也不替代实验室等级判定。
- 光学画面保持白色精密仪器语言，主体使用中性雾白摄影棚；物理高光与火彩来自当前凸多面体的光线求交和环境采样，不以普通高光贴图冒充光学结果。

## PDF 图层示意

- 参数页按保存图层逐组输出，不把同一区域的 C1/C2 或 P1/P2 合并成一张连续大表；超出单页容量时同组分页。
- 每个图层页都保留对应示意图：冠部和台面使用顶视，亭部使用底视，腰部使用正视；该图层仍存在的真实面以工作台粉色半透明填充和粉色边界标识。
- 示意图、组参数和逐面表必须出现在同一页，分页页仍重复示意图，保证数据与空间位置可以直接对应。

## 操作手册

- 操作手册与单个切型的技术报告职责分开：技术报告服务工艺交付，操作手册服务产品学习与培训。
- 手册固定使用 A4 纵向、真实工作台截图、可搜索中文字体、黑色发丝线、粉色章节标识与绿色提示块；用目录、连续章节编号、截图标注、任务步骤、局部说明和检查表建立阅读节奏，不得退化为平均分配的“整张截图加短说明”。
- 光学仿真图例必须先载入经过验证的完成切型，并至少提供透视与一个正交观察视图；不得再使用默认毛坯充当光学成果图。
- 手册源截图保存在 `docs/manual/screenshots/`，至少使用 2× 像素密度，必须完整呈现整页或功能区；排版使用等比完整适配，不得为填满画框裁掉图标、控件或面板边界。输出固定为 `public/manual/facet-96-operation-manual.pdf`，版本读取 `package.json`；运行 `npm run manual:build` 重新生成。
