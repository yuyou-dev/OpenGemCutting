# 预设琢型库

预设库是现有切磨工作台的资料入口，不是新的工作区。它从“文件 → 浏览预设琢型”打开居中浏览层，让用户先用外形、名称、设计者或来源查找，再检查真实几何四视图和基础参数，最后明确载入。

## 产品决策

- 画布、参数抽屉和 `CUT STACK` 继续是主任务；预设浏览层按需出现，关闭后不留下常驻导航。
- 首批只收录 `50–100` 个能被当前 96 分度领域模型准确表达的资料。数量不能凌驾于几何完整性、可追溯性和去重质量。
- 每项显示 `45° 轴测 / 顶视 / 底视 / 正视`。四张 SVG 由构建脚本对实际裁切实体投影生成，统一白底黑色细线，与 PDF 工程视图同属一种视觉语言。
- 载入会替换当前文档，并作为一条命令进入现有历史；一次撤销即可回到载入前文档。打开、筛选、预览和取消不修改文档。

## 质量门槛

`scripts/build-preset-library.mjs` 使用正式 `inspectGemCadAsc` 解析收集档案，并拒绝以下情况：

- 解析错误、preform、缺少或存在多个台面，以及无法精确映射到 96 齿的索引；
- 裁切后仍残留毛坯立方体面、冗余 tier、未知记录或重复索引；
- 空实体、异常体积/比例，或与已选资料几何完全相同；
- 缺少下载清单中的设计者、开放状态、来源页和 SHA-256 追溯链时，不得擅自补写来源信息。

解析器和数值门槛不能代替最终视觉验收。对解析有效但最终实体仍出现明显破面的资料，使用构建脚本中的显式策展排除清单；当前排除 `PC 02.276 Small OMNI Oval 1.4` 与 `PC 08.087B Chevron Cushion CC Brilliant 1.10`，重新生成 catalog 时不得回流。

构建结果写入 `public/presets/`：`catalog.json` 只含浏览摘要，单项规范化文档和四视图独立存放并按需请求。生成过程不把原始 ASC 档案、临时收集目录或本机绝对路径复制进仓库。

## 可延续结构

`src/domain/presetLibrary.js` 定义 provider 边界：

- `list()` 返回统一摘要；
- `load(summary)` 返回可导入的 Facet 96 JSON 文档；
- `save(document, metadata)` 是可选能力，供未来个人资料源使用。

当前 `builtin` provider 读取只读静态资源。未来“导入 ASC/JSON 到我的预设”和“将当前文档存为预设”应新增可写的个人 provider（例如 IndexedDB），复用同一浏览 UI、搜索筛选和载入命令，不修改内置 catalog，也不复制一套对话框。若增加来源切换，只在现有工具栏加入轻量来源筛选。

## 批量更新

```bash
npm run presets:build -- --archive /absolute/path/to/facetdiagrams-open-archive --limit 72
npm run check
```

正式生成默认要求至少 50 项、最多 100 项；只有开发 UI 时可临时使用 `--allow-partial`，不得用部分 catalog 形成里程碑提交。
