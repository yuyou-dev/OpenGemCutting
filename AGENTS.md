# Facet 96 Editor — Agent Instructions

## 工作方式

- 主动运行本地服务并在环境内浏览器验证；不要把可代执行的启动步骤留给用户。
- 开发和预览只能绑定 `127.0.0.1`，使用操作系统分配的高位临时端口。
- UI 代码放在 `src/`。保持 `.openai/hosting.json`、`worker/index.js`、`scripts/prepare-sites-build.mjs` 和 `tests/sites-worker.test.mjs` 的 Sites 交付链路可用。
- 修改前先检查脏工作区并保留用户已有改动。实现优先简洁，不引入过度防御性抽象。
- 大幅视觉改动且目标不明确时先使用 Product Design 能力获取上下文；从选定效果图实现时，把效果图作为布局、密度、间距、颜色、字体、内容和层级的视觉真值。
- 用户确认的长期产品决策写入本文件或 `design-system.md`；一次性 QA 证据放在被忽略的 `tmp/`，不要提交临时报告。

## 验收

- 常规与 Sites 交付统一运行 `npm run check`；`npm run test:sites` 仅用于单独诊断 Sites 产物。
- 构建必须留下 `dist/client/index.html`、`dist/server/index.js` 和 `dist/.openai/hosting.json`。
- 仓库不保留浏览器自动化依赖。需要截图回归时用 `npm i --no-save puppeteer-core` 和系统 Chrome，脚本写入 `tmp/`；WebGL 无头测试使用 `--use-angle=swiftshader`。

## 产品与视觉真值

- 整体基准为 `docs/assets/design/selected-option-2.png`（Axial Workbench），命令条基准为 `docs/assets/design/toolbar-option-3.png`；详细规范见 `design-system.md`。
- 保持白色、p5.js 风格的精密仪器语言：细发丝线、紧凑双语标签、克制字体、分组折叠和轻量浮动表面。
- 正文与辅助标签的最小字号为 `9px`，操作按钮不小于 `10px`；不得为了压缩密度牺牲可读性。
- 工作区只有一个永久左侧参数抽屉；3D 画布为主区域。`CUT STACK` 与命令条组成对齐的顶部浮动轨道，统一使用 `42px / 6px / 30px` 尺寸 token。
- 顶部只保留视图、可编辑切型名称与面数、显示模式、文件、撤销/重做和更多菜单。显示模式菜单可进入临时的聚焦光学仿真；不得恢复独立常驻的“光学”“参数”工作区按钮。JSON 与 GemCad ASC 导入导出仍在文件菜单。
- GemCad ASC 导入导出必须先显示持久预检：源齿轮、96 齿精确映射、统一几何比例、L/W、层/面统计及可恢复警告。任何 `(index - gearLocation) × 96 / gear` 非整数映射、preform、缺少 0° 台面或存在多个 0° 台面都必须阻断，不能静默取整或虚构层。
- 预设琢型库只从文件菜单打开居中浏览层，不新增常驻工作区。首批内置资料保持 `50–100` 个，经同一 ASC 解析器、精确 96 齿映射、有效实体、完整去重和来源记录筛选；每项预先生成真实几何的白底黑线 `45° 轴测 / 顶视 / 底视 / 正视` 四视图。
- 对解析虽通过但最终几何仍出现肉眼可见破面的资料，必须加入 `scripts/build-preset-library.mjs` 的显式策展排除清单；重新生成 catalog 时不得回流。
- 预设 UI 只依赖 `list / load / 可选 save` 的 provider 契约。内置静态资料、未来个人 ASC/JSON 和“当前文档存为预设”必须沿这一契约扩展，不能把个人资料逻辑耦合到内置 catalog。载入预设作为一次可撤销的文档替换命令。
- 切型名称是文档元数据，不是独立 UI 状态。命令条内联输入以 `Enter` 或失焦提交、`Escape` 取消，并通过统一文档命令进入撤销/重做；输入框键盘事件不得冒泡影响 CUT 会话。JSON、ASC 与 PDF 文件名、报告标题统一读取 `document.name`。
- 冠部/腰部/亭部切换属于 `CUT STACK`。历史和刻面台账按需浮动打开；台账始终是居中覆盖层。
- 品牌固定为黑色“切磨工作台”，`Alpha` 和产品线为灰色；拼写为 `SUVA`，不得写成 `SUWA`。
- 首次加载必须在 JavaScript 模块和字体准备期间显示白底品牌启动页，使用真实品牌标志、`切磨工作台 / Alpha / SUVA · FACET 96` 与克制的粉色进度反馈；启动页不得伪装业务进度，资源就绪后短暂淡出，并服从 `prefers-reduced-motion`。
- 帮助入口固定在“更多工具”，不得增加常驻导航。帮助中心按“快速开始、切割工作流、文件与交换、光学仿真、视口与快捷键”组织，可按 `Escape` 退出且不改变文档或 CUT 会话；完整 A4 操作手册固定由 `public/manual/facet-96-operation-manual.pdf` 提供。
- 操作手册使用 `docs/manual/screenshots/` 的真实界面截图和现有报告品牌语言，由 `npm run manual:build` 可重复生成。封面只展示完整品牌身份而不使用正文页头，第 2 页起每页页头必须包含品牌标识与产品线，结尾保留开源许可、权利边界和第三方致谢。界面、产品规则或版本号变化时必须同步截图与手册，不允许以占位图或旧界面充数；截图至少使用 2× 像素密度，只能取完整页面或完整功能区，不得裁掉图标、控件或面板边界；光学页面只使用经过验证的完成切型，并提供至少两种观察视角。
- 聚焦光学仿真以 `docs/assets/design/optics-focus-simulation.png` 为视觉真值：进入后隐藏参数抽屉、`CUT STACK`、图层与全部 CUT helper，仅显示当前真实/预览实体、仿真命令条和可收起的右侧光学检查器；退出或按 `Escape` 必须恢复进入前的完整编辑现场。仿真不是 CUT 第五态，进入与退出不得提交、取消或写入历史。

## 几何与文档模型

- 使用凸多面体半空间裁切，保留 `normal · point <= offset`。+Z 朝冠部：冠部 β 为正，腰部为 0，亭部为负；旧 JSON 导入时重新解析几何以保持兼容。
- 保存的 `CUT STACK` 是唯一几何数据源。每个 `T/C/G/P` 图层是一份不可变参数快照，按列表顺序应用一次；编辑保存必须在原序列位置替换。
- 每个文档从边长 `2.000`、轴心在原点的立方体开始，并默认包含：固定首层 `T1 台面`（角度锁定 0°、深度可调、不可隐藏/删除）与可编辑的 32 折 `G1 腰部`（深度 0.2）。清除切割恢复这两层。
- 水平分度为整数 `0..95`，显示值 `96` 与内部 `0` 同位。行业角连续且内部保存全精度，滑杆、数值框、行内编辑与 3D 手柄的最小调整步进统一为 `0.01°`，UI 至少显示两位小数；切入深度独立且保存全精度，只在 UI 显示到 0.001。
- 冠部和亭部默认 8 折，腰部默认 16 折；镜像轴偏移默认 0。N 折必须产生 N 个有效裁切面，深度从毛坯的 Z 轴旋转包络测量，不能因方形毛坯退化成四面。
- 镜像采用二面体定义：N 折拥有分布于 180° 的 N 条无向反射轴。旋转面保留；镜像偏移可增加第二组 N 面轨道，重合面去重。
- 每个面记录索引、行业角、带符号 β、深度、方位角、所属图层和显式裁切平面。撤销/重做、显隐、重排和导入导出均必须保持参数化图层语义。
- ASC 的 center-to-facet distance 不是内部深度。导入时整份文件使用统一比例换算到边长 2.000 毛坯和非负旋转包络深度，保持角度、平面距离比、L/W 与高度比例；每个 `a` tier 作为一份 arbitrary 图层快照。固定 T1 可为内部首层，但必须保存 ASC 原序号供再次导出恢复。

## 编辑状态与安全规则

- “新建动作”和“编辑图层”语义不同。选择已保存图层只恢复参数，参数真正变化前不得改变几何。
- 编辑中的图层不能原地改区域。点击不同区域会退出编辑，并以该区域默认值开始新草稿；已有图层必须保留。
- 新建文档和新建动作立即显示草稿切面；空闲态区域切换只改变筛选，编辑/新建态区域切换才开始新草稿。选择未修改的已保存层时实体保持已提交几何，但仍显示活动主切割面与 Gizmo；参数变化后才显示未保存实体预览。helper 在实体之后绘制，略沿法线偏移以避免 z-fighting，并正确受实体遮挡。
- 保存前的破坏检查独立于构造 helper：若新参数会抹除任何已提交图层的面，必须拒绝保存并提示受威胁图层。
- 冠部整体升降必须连同固定台面一起沿世界 Z 轴移动；亭部整体升降只作用于全部亭部层。批量位移写回各层深度/裁切平面并合并为一次撤销，台面原有的单层深度编辑保持不变。
- 冠部/亭部群组工具统一为一个“整体变换”会话，在一次预览与一次撤销中组合 `ΔZ` 整体升降、以腰线为基准的世界 Z 轴高度比例和整数 96 分度旋转。冠部包含固定台面；比例必须大于零且不得穿越腰线。所有目标层同步重算行业角、深度和裁切平面，同时保持重复、镜像、层序和拓扑关系；旋转只对非台面目标层的 `baseIndex/index` 加同一齿数并对 96 取模，固定水平台面不旋转。
- 群组操纵杆使用紫色升降面/手柄、青绿色比例面/手柄、暖金色 96 齿旋转环和石墨灰腰线基准面，命中优先级为旋转环、独立手柄、控制面。升降控制面位于冠部腰线上沿或亭部腰线下沿，`ΔZ` 数值框只用于精确输入。实体模式下全部群组 helper 统一服从实体深度遮挡；被遮住部分不得在屏幕层重画或响应拖拽。
- 新增或更新图层保存成功后必须退出编辑并释放活动 Gizmo；只有用户主动选择已保存层时才重新进入编辑。活动编辑层的真实刻面使用淡粉选中色，与悬停反馈和普通材质明确区分。
- CUT 交互统一为四态：空闲、新建、编辑、群组，并由 `src/domain/cutSession.js` 中的单一事件驱动状态机统一管理状态身份、脏数据、相遇目标、进入/退出与权限；声明式配置表驱动参数、Gizmo、半透明切割面、图层高亮、提交动作和新建入口。所有非空闲态必须同时提供显式取消和 `Escape` 退出，取消不写历史且完整释放草稿、相遇目标与 Gizmo。空闲态禁用参数并隐藏提交按钮，只能从 `CUT STACK` 底部绿色会话槽位进入新建；新建与编辑态在同一槽位原位显示粉色状态、面数与退出操作，标题栏和参数抽屉不再重复提供会话状态。空闲或群组态点击顶点/棱不得隐式创建草稿，活动会话期间不得通过点面或图层列表静默切换状态。新建与编辑态必须同时显示 Gizmo 和主切割面，编辑态必须同步选中对应图层并以粉色填充和边界标识活动面；只有存在新建预览或编辑参数已变化时才能提交，行内编辑只以回车或主保存按钮明确提交，不得在失焦时抢先保存。
- `src/domain/meet.js` 保持无 UI 依赖。点选顶点/棱只在构造期求解数值，保存层仍只含普通数值参数。

## 控件与 3D helper

- 行业角、深度、96 分度、重复、镜像轴偏移和自定义索引放在同一个紧凑 CUT 参数区。抽屉唯一的新建入口是 `CUT STACK` 的 `+`。
- `IndexTape` 为单行水平 96 齿标尺：点击/拖动吸附整数，步进 ±1，读数支持精确输入。
- 活动 CUT 使用 `docs/assets/design/gizmo-radial-gimbal-bridge.png` 确定的串联机械臂：蓝色 0–90° 桥架位于当前索引的竖直径向平面内，桥架半径必须与外圈 96 分度圆半径完全相同，因此水平整圆和竖直四分之一圆共用 `[0,0,0]` 圆心；其 90° 端点以竖直切线直接落在外圈固定轴承上，中间不得再加连接段。粉色直杆从桥架滑块沿圆弧半径方向伸出，始终垂直圆弧局部切线，同时作为切割平面法线；平面与直杆垂直，深度只让整张平面沿法线平移，端点圆标只是平面控制柄而非多面体 meet 点。台面和腰部角度锁定；开始角度拖拽时解除 meet 目标，不能隐式重算深度。
- 深度无固定 UI 上限，保持非负；滑块范围依据毛坯和当前值扩展。即使暂时切得过深，预览和 helper 仍需可见并能拖回。
- 96 分度 helper 为世界空间同心双环：石墨外圈编辑基础索引，暖金内圈编辑镜像轴偏移。旋转轨道用圆点，镜像轨道用菱形；只绘制一条代表性反射直径，其余轴以成对缺口表达。
- N 折重复的全部 N 个切面始终参与几何裁切，但实体模式只把 `baseIndex` 对应成员显示为红色主编辑平面；其他重复成员仅在需要时作为次级/X-ray 提示。Gizmo 的法线、深度杆和主平面必须按 `baseIndex` 查找，绝不能依赖按显示索引排序后的第一个成员。镜像偏移产生的第二轨道也不得夺取主 Gizmo。
- helper 随宝石旋转，数值徽标保持屏幕可读；重复层只显示主成员的一套控制器。3D 几何和 helper 使用固定屏幕空间细线，不随缩放显得变粗。
- 顶视从冠部向下看，`pitch = -π/2`。正交视图使用正 near plane。可聚焦画布不得出现全区域蓝色 focus outline，但键盘交互必须保留。
- `CUT STACK` 支持悬停高亮、视口点面选层、双击改名和非台面层拖拽重排。
- `CUT STACK` 的新建图层入口固定在图层列表底部，不放在标题栏；仅在没有编辑已有层、没有未保存草稿且不处于群组调整时显示，保存完成回到空闲态后重新出现。新建草稿的真实刻面使用淡蓝预览色，编辑已有层继续使用淡粉选中色。
- `CUT STACK` 列表底部使用固定高度的会话槽位：空闲态是低饱和绿色整行新建入口，新建/编辑态原位变为粉色状态条并提供退出按钮；文案由 CUT 会话状态派生，标题栏和左侧参数抽屉不重复显示会话退出或状态，底部保存/更新按钮不移动。
- 编辑态活动图层不能因禁用态全局透明度而变灰：选中行的名称、参数摘要和面数必须保持深色与完整不透明度；只有其他暂不可操作图层使用浅灰弱化。

## 切割指令与报告

- 抽屉切割指令列出全部图层并分为亭部、腰部、冠部三栏，台面放在冠部末尾。每行是粗体操作前缀、两位小数行业角和完整的连字符索引串；当前草稿/编辑行使用克制选中色。
- PDF 为 A4 矢量优先技术报告：品牌封面、真实几何的顶/底/正/侧/标准 45° 轴测视图、工程双向尺寸箭头和按保存图层独立分页的逐面表。每个 C/P/G/T 图层单独成组并附示意图：冠部与台面用顶视、亭部用底视、腰部用正视，对应真实面以工作台粉色高亮；同组分页时重复示意图。文字需嵌入可搜索中文字体，只有品牌标记可栅格化。
- 顶/底视图分开；正/侧视图只画观察者可见几何并标注台面宽度 `T`。报告字段包括索引、行业角、带符号 β、深度、方位角和裁切平面。
- PDF 默认省略腰部逐面表，但几何视图仍含腰部并注明省略；导出可显式包含。JSON 始终完整。
- ASC 只交换已保存 CUT STACK 的显式 96 齿平面与折射率；不保存历史、隐藏状态、毛坯、会话及其他光学元数据。导出前必须列出范围与重复/镜像展开等不可逆信息，JSON 继续作为完整主文件。

## 仓库

- OpenGemCutting 是独立的公开发行仓库，保留自己的 Git 历史、MIT License、公开远程和发布节奏；不得混入任何内部仓库名称、私有远程、handoff、个人绝对路径、账号、密钥或未授权素材。
- `tmp/`、`output/`、`dist/` 与本机依赖不进入版本控制。长期文档资产放在 `docs/`，公开静态资源放在 `public/`。
- 每次发布前运行敏感信息扫描、依赖审计、`npm run check`、`npm run build:pages` 与浏览器验收；确认 GitHub Pages、手册下载和静态资源均可访问。
